services:
  app:
    build: .
    container_name: doxora-app
    expose:
      - "3000"
    environment:
      - NODE_ENV=production
    env_file: .env.production
    restart: unless-stopped
    networks:
      - doxora-network

  nginx:
    image: nginx:alpine
    container_name: doxora-nginx
    ports:
      - "3000:80"
      - "8443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./ssl:/etc/nginx/ssl:ro
    depends_on:
      - app
    restart: unless-stopped
    networks:
      - doxora-network

networks:
  doxora-network:
    driver: bridge
